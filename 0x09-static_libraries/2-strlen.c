#include <stdio.h>
#include <string.h>
/**
 * _strlen - cont the lenght of a name
 * @s: the name value
 * Return: a variable, the lenght of the name
 */
int _strlen(char *s)
{
	int i;

	i = strlen(s);
	return (i);
}
