#include <stdio.h>
/**
 * _strlen - cont the lenght of a name
 * @s: the name value
 * Return: a variable, the lenght of the name
 */
int _strlen(char *s)
{
	int i, a;

	for (i = 0; i <= strlen(s); i++)
	{
		a = i;
	}
	return (a);
}
